import{useEffect,useState}from"react";import{supabase}from"./supabase.js";import Dashboard from"./pages/Dashboard.jsx";import Login from"./pages/Login.jsx";export default function App(){const[session,setSession]=useState(null);const[ready,setReady]=useState(false);const[route,setRoute]=useState(window.location.pathname);const nav=p=>{window.history.pushState({},'',p);setRoute(p);};useEffect(()=>{supabase.auth.getSession().then(({data})=>{setSession(data.session);setReady(true);});const{data:l}=supabase.auth.onAuthStateChange((_e,s)=>setSession(s));return()=>l.subscription.unsubscribe();},[]);if(!ready)return<div style={{minHeight:"100vh",background:"#0a0a0a",color:"#f0ece4",display:"flex",alignItems:"center",justifyContent:"center"}}>Lade...</div>;if(route==="/demo")return<Dashboard onNavigate={nav} onLogout={()=>nav("/")} session={{user:{id:"demo-user",email:"demo@bauabnahme.ch"}}} isDemo={true}/>;if(!session)return<Login onNavigate={nav}/>;return<Dashboard onNavigate={nav} onLogout={async()=>{await supabase.auth.signOut();nav("/");}} session={session}/>;}
